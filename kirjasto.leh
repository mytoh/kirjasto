

(install
  (
   (make-directory* (build-path ( *lehti-dist-directory* ) "kirjasto"))
   (copy-directory* (build-path ( *lehti-cache-directory* ) "kirjasto/skripti")
                    (build-path ( *lehti-dist-directory* ) "kirjasto/skripti"))
   ;; library sources
   (copy-directory* (build-path ( *lehti-cache-directory* ) "kirjasto/lib")
                    (build-path ( *lehti-dist-directory* ) "kirjasto/lib"))))
